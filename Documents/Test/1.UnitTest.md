# Bài 1: Testing là gì - Vì sao cần viết test

## Khởi động: Một tình huống thực tế

Bạn có một API `/products` trả về danh sách sản phẩm. Sau khi thêm tính năng phân trang, bạn phải:

- Gọi API thủ công bằng Postman.

- Kiểm tra response JSON.

- Đổi query params, test lại.

- Nếu có bug, fix code → lại test thủ công.

👉 Mỗi lần thay đổi code, bạn phải test lại từ đầu → tốn thời gian, dễ sót lỗi.

## Testing là gì?

**Testing trong lập trình**: Quá trình xác minh rằng code hoạt động đúng như mong đợi.

Có 2 cách tiếp cận:

- **Manual test (kiểm thử thủ công)**: dùng tay để test (Postman, browser...).

- **Automated test (kiểm thử tự động)**: Viết code test (Unit test, Integration test, E2E test...)

Ví dụ đơn giản (Nodejs Javascript):

```javascript
const assert = require('assert')
const { test } = require('node:test')

function sum(a, b) {
  return a + b
}

// Manual test: console.log(sum(2, 3)) → 5
// Automated test:
test('sum should add two numbers', () => {
  assert.strictEqual(sum(2, 3), 5)
})
```

## Vì sao cần viết test (thay vì chỉ manual test)?

1. Tiết kiệm thời gian về lâu dài

2. Giảm rủi ro bug trong production

3. Tự tin refactor code

4. Tài liệu sống cho code